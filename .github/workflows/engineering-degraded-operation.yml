name: AASM Engineering Degraded Operation Qualification

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: aasm-engineering-degraded-operation-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  statuses: write

jobs:
  engineering-degraded-operation:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.13"
          cache: pip
      - name: Install AASM development dependencies
        run: python -m pip install -e '.[dev]'
      - name: Compile S4.5 degraded-operation foundation
        run: |
          python -m compileall -q \
            src/aasm/degraded_operation.py \
            scripts/check_degraded_operation_contracts.py \
            tests/test_degraded_operation_foundation.py
          python -m json.tool schemas/degraded-operation.schema.json >/dev/null
          python -m json.tool schemas/degraded-operation-assessment.schema.json >/dev/null
      - name: Check S4.5 source and pre-admission firewalls
        run: python scripts/check_degraded_operation_contracts.py
      - name: Run S4.5 adversarial foundation corpus
        run: pytest -q tests/test_degraded_operation_foundation.py
      - name: Publish S4.5 qualification
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          JOB_STATUS: ${{ job.status }}
        shell: bash
        run: |
          set -euo pipefail
          if [ "$JOB_STATUS" = success ]; then
            state=success
            description='S4.5 degraded-operation pre-admission foundation passed'
          else
            state=failure
            description='S4.5 degraded-operation foundation failed'
          fi
          for attempt in 1 2 3 4 5; do
            if gh api "repos/$GITHUB_REPOSITORY/statuses/$GITHUB_SHA" \
              -f state="$state" \
              -f context='aasm/engineering-degraded-operation' \
              -f description="$description" \
              -f target_url="https://github.com/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID"; then
              exit 0
            fi
            if [ "$attempt" -lt 5 ]; then sleep $((attempt * 2)); fi
          done
          echo 'failed to publish aasm/engineering-degraded-operation after bounded retries' >&2
          exit 1
