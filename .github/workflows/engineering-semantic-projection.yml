name: AASM Semantic Projection and Equivalence Qualification

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: aasm-engineering-semantic-projection-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  statuses: write

jobs:
  semantic-projection:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.13"
          cache: pip
      - name: Install AASM development dependencies
        run: python -m pip install -e '.[dev]'
      - name: Compile semantic projection/equivalence foundation
        run: |
          python -m compileall -q \
            src/aasm/semantic_projection.py \
            src/aasm/_semantic_projection_core.py \
            src/aasm/_semantic_projection_equivalence.py \
            tests/test_semantic_projection_foundation.py \
            tests/test_semantic_projection_textpcb.py \
            tests/test_semantic_projection_adversarial.py \
            scripts/check_semantic_projection_contracts.py
          python -m json.tool schemas/semantic-projection.schema.json >/dev/null
      - name: Check substrate reconciliation and pre-admission firewalls
        run: python scripts/check_semantic_projection_contracts.py
      - name: Run semantic projection/equivalence adversarial corpus
        run: pytest -q tests/test_semantic_projection_foundation.py tests/test_semantic_projection_textpcb.py tests/test_semantic_projection_adversarial.py
      - name: Publish semantic projection/equivalence qualification
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          JOB_STATUS: ${{ job.status }}
        shell: bash
        run: |
          set -euo pipefail
          if [ "$JOB_STATUS" = success ]; then
            state=success
            description='S4 semantic projection/equivalence pre-admission foundation passed'
          else
            state=failure
            description='S4 semantic projection/equivalence pre-admission foundation failed'
          fi
          for attempt in 1 2 3 4 5; do
            if gh api "repos/$GITHUB_REPOSITORY/statuses/$GITHUB_SHA" \
              -f state="$state" \
              -f context='aasm/engineering-semantic-projection' \
              -f description="$description" \
              -f target_url="https://github.com/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID"; then
              exit 0
            fi
            if [ "$attempt" -lt 5 ]; then sleep $((attempt * 2)); fi
          done
          echo 'failed to publish aasm/engineering-semantic-projection after bounded retries' >&2
          exit 1
