name: Final v0.53 Inventory Refresh

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  refresh:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v7
        with:
          python-version: '3.13'
      - name: Remove helper first, then regenerate exact inventory
        run: |
          set -euo pipefail
          git rm .github/workflows/refresh-v53-inventory-final.yml
          python scripts/release_manifest.py --write-file-list
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add FILE_LIST.txt
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m 'Finalize canonical inventory for v0.53 foundation'
          git push origin HEAD:main
